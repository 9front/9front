#!/bin/rc
# usbstart - start appropriate usb flavour
if(test -r '#u'/usb) {
	if(! test -r /dev/usb)
		bind -a '#u' /dev
	# /boot/boot may have started usbd, which starts all usb drivers
	if(test -f /srv/usb)
		mount -a /srv/usb /dev
	if not
		usb/usbd
}
exit ''
